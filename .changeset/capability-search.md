---
"@resciencelab/dap": minor
---

Add capability-based peer discovery: `findPeersByCapability()` in peer-db, `capability` parameter on `p2p_list_peers` agent tool, and `--capability` flag on `p2p-peers` CLI command
